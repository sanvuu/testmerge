name: Python application

env:
  DOCKER_USERNAME: sanvu
  DOCKER_PASSWORD: ${{ secrets.PASS1 }}
  IMAGE_TAG: ${{ github.sha }}

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Docker Login
      run: docker login -u ${DOCKER_USERNAME} -p ${DOCKER_PASSWORD}

    - name: Docker Build
      run: docker build -f Dockerfile -t ${DOCKER_USERNAME}/testmerge/py:${IMAGE_TAG} ./py/

    - name: docker push
      run: docker push ${DOCKER_USERNAME}/testmerge/py:${IMAGE_TAG}
